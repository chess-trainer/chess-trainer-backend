import sqlite3

conn = sqlite3.connect('user.db')

cursor = conn.cursor()

cursor.execute("""CREATE TABLE users (
                id int,
                handle text,
                password text,
                email text,
                puzzle integer
                puzzle_ratings integer
                )""")

conn.commit()

conn.close()